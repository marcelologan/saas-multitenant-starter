/* resources/css/themes/themes.css */

/* 
|--------------------------------------------------------------------------
| Sistema de Temas Dinâmico
|--------------------------------------------------------------------------
|
| Este arquivo agora será gerado dinamicamente baseado na configuração
| em config/theme.php. As variáveis CSS serão injetadas via PHP.
|
*/

/* Variáveis CSS serão injetadas aqui via PHP */
/* Ver: ThemeHelper::generateCssVariables() */

/* Classes Utilitárias que usam as variáveis */
.bg-gradient-primary {
  background: var(--gradient-primary);
}

.bg-gradient-hero {
  background: var(--gradient-hero);
}

.glass-effect {
  background: rgba(255, 255, 255, 0.1);
  backdrop-filter: blur(10px);
  border: 1px solid rgba(255, 255, 255, 0.2);
}

.card-hover {
  transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
}

.card-hover:hover {
  transform: translateY(-4px);
  box-shadow: var(--shadow-xl);
}

.btn-gradient {
  background: var(--gradient-primary);
  color: white;
  border: none;
  transition: all 0.3s ease;
}

.btn-gradient:hover {
  transform: translateY(-1px);
  box-shadow: var(--shadow-lg);
}

/* Utilitários para cores principais */
.text-primary { color: var(--primary); }
.text-secondary { color: var(--secondary); }
.text-accent { color: var(--accent); }
.text-success { color: var(--success); }
.text-warning { color: var(--warning); }
.text-danger { color: var(--danger); }
.text-info { color: var(--info); }

.bg-primary { background-color: var(--primary); }
.bg-secondary { background-color: var(--secondary); }
.bg-accent { background-color: var(--accent); }
.bg-success { background-color: var(--success); }
.bg-warning { background-color: var(--warning); }
.bg-danger { background-color: var(--danger); }
.bg-info { background-color: var(--info); }

.border-primary { border-color: var(--primary); }
.border-secondary { border-color: var(--secondary); }
.border-accent { border-color: var(--accent); }
.border-success { border-color: var(--success); }
.border-warning { border-color: var(--warning); }
.border-danger { border-color: var(--danger); }
.border-info { border-color: var(--info); }

/* Hover states */
.hover\:bg-primary:hover { background-color: var(--primary-hover); }
.hover\:bg-secondary:hover { background-color: var(--secondary-hover); }
.hover\:bg-accent:hover { background-color: var(--accent-hover); }
.hover\:bg-success:hover { background-color: var(--success-hover); }
.hover\:bg-warning:hover { background-color: var(--warning-hover); }
.hover\:bg-danger:hover { background-color: var(--danger-hover); }
.hover\:bg-info:hover { background-color: var(--info-hover); }

.hover\:text-primary:hover { color: var(--primary-hover); }
.hover\:text-secondary:hover { color: var(--secondary-hover); }
.hover\:text-accent:hover { color: var(--accent-hover); }